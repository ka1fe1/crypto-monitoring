## 新增需求

### 需求: 遵守防打扰静默时间
Polymarket 每日报告任务不得在用户配置的防打扰静默时间内执行。

#### 场景: 任务在静默时间内触发
- **当 (WHEN)** 当前时间处于配置的静默时间启动和结束时间之内时
- **则 (THEN)** 任务应中止执行，且不生成任何报告或警报。

#### 场景: 任务在静默时间外触发
- **当 (WHEN)** 当前时间超出配置的静默时间范围时
- **则 (THEN)** 任务应正常执行并生成每日报告。
